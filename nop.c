#include "monty.h"

/**
 * nop - a function that do nothing
 * @stack: a doubly linked list pointer
 * @line_number: a line number of a monty file
 * Return: none
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
